测试步骤：
	1. EchoServer服务启动，测试监听了8080端口
	2. 使用telnet作为客户端，进行测试
		telnet 127.0.0.1 8080
		
	
	telnet设置回显字符：
		(1) telnet 127.0.0.1 8080
		(2) 同时按下："Ctrl" + "]"
		(3) 输入：set localecho
		(4) 回车
		(5) 回车, 此时返回到一个新的telnet操作界面，这时可以看到是回填的字符了。
		
	